/**
 * Placeholder. The Google OAuth button, the redirect handling and the auth
 * state are built in Phase 1 of ROADMAP.md.
 */
export function Login() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-sm flex-col justify-center px-4">
      <h1 className="text-xl font-semibold tracking-tight">Campaign Mailer</h1>
      <p className="mt-2 text-sm text-ink-muted">
        Connexion Google — implémentée en Phase 1.
      </p>
    </main>
  )
}
